# Migrations

Run deployment-time schema changes from this directory.

Example:

```bash
php8.3 migrations/001_lesson_text_support.php
```

Keep runtime application files focused on request handling and configuration only.
